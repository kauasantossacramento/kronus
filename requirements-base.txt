# ══════════════════════════════════════════════════════════════
# Kronus — dependências base (sem stack de visão computacional)
# ══════════════════════════════════════════════════════════════

# ── Core ──────────────────────────────────────────────────────
Django>=5.1,<6.0
djangorestframework>=3.15
djangorestframework-simplejwt>=5.3
django-cors-headers>=4.3
django-filter>=24.0
django-extensions>=3.2
django-storages>=1.14
django-redis>=5.4

# ── Segurança ─────────────────────────────────────────────────
# Argon2 é o primeiro PASSWORD_HASHER (Seção 9 do plano). Sem este
# pacote o Django levanta ImproperlyConfigured no primeiro hash de
# senha — não é opcional.
argon2-cffi>=23.1

# ── Async / Realtime ──────────────────────────────────────────
celery>=5.4
redis>=5.0
channels>=4.1
channels-redis>=4.2
daphne>=4.1

# ── Banco de dados ────────────────────────────────────────────
psycopg2-binary>=2.9

# ── Documentos / Relatórios ───────────────────────────────────
WeasyPrint>=62.0
openpyxl>=3.1
Pillow>=10.2
qrcode>=7.4

# ── API Docs ──────────────────────────────────────────────────
drf-spectacular>=0.27

# ── Infra / Deploy ────────────────────────────────────────────
python-decouple>=3.8
gunicorn>=22.0
whitenoise>=6.6
sentry-sdk>=1.40
boto3>=1.34

# ── Numérico (embeddings faciais) ─────────────────────────────
numpy>=1.26
